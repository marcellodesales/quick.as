extend layout
block content
  ul
    li
      a(href='/api/v1/casts/setup') Setup
      p GET. Drop and Create table 'casts, tags, casts_tags'
    li 
      a(href='/api/v1/casts/publish') Publish
      p GET. Headers: token. Responds with JSON (temp amazon access keys, requesting user details and cast id) or 401